@addTagHelper "*, Microsoft.AspNetCore.Mvc.TagHelpers"
@model OrcaStarsWebApplication.ViewModels.ApplicationViewModel
@inject OrcaStarsWebApplication.Models.AddressService Address

@{
    ViewData["Title"] = "Confirm Display";
    Layout = "_Layout";
}

@section Styles {
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.13.0/css/all.css" />
    <link rel="stylesheet" href="~/css/confirmation.css" />

    <style>
        #designs, #text-center {
            background-color: white;
            color: black;
        }
        #designs a, #designs h1, #designs h2, #designs h3, #designs p,
        #text-center a, #text-center h1, #text-center h2, #text-center h3, #text-center p {
            color: black;
        }
        .phone {
            overflow: hidden;
            width: 350px;
            height: 600px;
            background-color: #005792;
            border: solid black 2px;
            border-radius: 4px;
        }

            .phone .bus_photo {
                position: inherit;
                width: 100%;
                height: 200px;
            }

            .phone .logo_photo {
                position: inherit;
                width: 100%;
                height: 200px;
            }

        .phone .content {
            width: 100%;
            background-image: linear-gradient(#0E85C7, #BED1E0);
        }

        .phone h2 {
            background-color: #005792;
            color: white;
            padding-left: 5px;
        }

        .content h3 {
            color: black;
            padding-left: 10px;
            font-size: 2em;
        }

        .description {
            height: 50px;
            padding-left: 10px;
        }
    </style>
}
<div class="text-center" id="text-center">
    <h1 class="display-4">Confirming</h1>
    <h3>Confirm Owner Information</h3>
    <p><b>Name:</b> @Model.FirstName @Model.LastName </p>
    <p><b>Phone:</b> @Model.PhoneNumber</p>
    <p><b>Email:</b> @Model.Email</p>
    <br />
    <h3>Confirm Business Information</h3>
    <p><b>Name:</b> @Model.BusinessName</p>
    <p><b>Description:</b> @Model.Description</p>
    <p><b>Hours:</b> @Model.Hours</p>
    <p><b>Website:</b> <a target="_blank" href="http://@Model.Website">@Model.Website</a></p>
    <p><b>Social Media:</b>
        <br />Facebook: @@@Model.Facebook
        <br />Twitter: @@@Model.Twitter
        <br />Instagram: @@@Model.Instagram
    </p>
    <p>
        <b>Business Logo: @Model.BusinessLogoHolder</b>
        <img src="~/@Model.BusinessLogoHolder" style="width:300px; height: auto;" />
    </p>
    <p>
        <b>Store Image: @Model.StoreLogoHolder</b>
        <img src="~/@Model.StoreLogoHolder" style="width:300px; height: auto;" />
    </p>
    <p><b>Location:</b> @Address.fieldsToAddress(@Model)</p>
    <div>
        <script type='text/javascript'>
            var map, searchManager;

                function GetMap() {
                map = new Microsoft.Maps.Map('#myMap', {
                    credentials: 'At1ht0A7Q_vav26C7rNXQPlNWmAwmbeIruD0BFWJXiV0yMJCkz4WuX9XbcIuNNf_'
                });
            }

            window.onload = function Search() {
                if (!searchManager) {
                    //Create an instance of the search manager and perform the search.
                    Microsoft.Maps.loadModule('Microsoft.Maps.Search', function () {
                        searchManager = new Microsoft.Maps.Search.SearchManager(map);
                        Search()
                    });
                } else {
                    //Remove any previous results from the map.
                    map.entities.clear();

                    //Get the query and geocode it.
                    var query = "@Address.fieldsToAddress(@Model)";
                    geocodeQuery(query);
                }
            }

            function geocodeQuery(query) {
                var searchRequest = {
                    where: query,
                    callback: function (r) {
                        if (r && r.results && r.results.length > 0) {
                            var pin, pins = [], locs = [], output = 'Results:<br/>';

                            for (var i = 0; i < r.results.length; i++) {
                                //Create a pushpin for each result.
                                pin = new Microsoft.Maps.Pushpin(r.results[i].location, {
                                    text: i + ''
                                });
                                pins.push(pin);
                                locs.push(r.results[i].location);

                                output += i + ') ' + r.results[i].name + '<br/>';
                            }

                            //Add the pins to the map
                            map.entities.push(pins);

                            //Display list of results
                            //document.getElementById('output').innerHTML = output;

                            //Determine a bounding box to best view the results.
                            var bounds;

                            if (r.results.length == 1) {
                                bounds = r.results[0].bestView;
                            }
                            else {
                                //Use the locations from the results to calculate a bounding box.
                                bounds = Microsoft.Maps.LocationRect.fromLocations(locs);
                            }

                            map.setView({ bounds: bounds });
                        }
                    },
                    errorCallback: function (e) {
                        //If there is an error, alert the user about it.
                        alert("Couldn't find the Address at @Address.fieldsToAddress(@Model)");
                    }
                };

                //Make the geocode request.
                searchManager.geocode(searchRequest);
            }
        </script>
        <script type='text/javascript' src='https://www.bing.com/api/maps/mapcontrol?callback=GetMap' async defer></script>
        <!--<input id='searchTbx' type='text' />-->
        <!--<input type='button' value='Search' onclick='Search()' />-->
        <div id="myMap" style="position:relative;width:600px;height:400px;"></div>
        <!--<div id='output' style="margin-left:10px;float:left;"></div>-->
    </div>
    <br />
    <!-- <input type="submit" class="submit-form-btn" value="Edit">-->
    <h1>Design Examples</h1>

</div>

<div class="webdisplay">
    <h1>Orca Stars</h1>
    <section class="intro" style="background:url(~/@Model.StoreLogoHolder);">
        <img src="~/@Model.StoreLogoHolder" />
        <div class="inner">
            <div class="content">
                <a href="#" class="btn">See All Photos</a>
            </div>
        </div>
    </section>
    <section class="footer">
        <div class="footer-content">
            <h1>@Model.BusinessName</h1>
            <p>
                @Model.Description
            </p>

            <div class="icons">
                <p>
                    <i class="fa fa-map-marker-alt"></i>
                    @Address.fieldsToAddress(@Model)
                </p>
                <p>
                    <i class="far fa-clock"></i>
                    9am-9pm, Mon-Fri
                </p>
            </div>
            <a href="#" class="btn">Directions</a>
        </div>
    </section>
</div>
<div id="designs">
    <h2>App Example</h2>
    <a>View Info</a>
    <div class="phone">
        <h2> OrcaStars </h2>
        <div class="content">
            <div>
                <img class="logo_photo" src="~/@Model.StoreLogoHolder" alt="logo_photo">
            </div>
            <div>
                <h3>@Model.BusinessName</h3>
                <span>*</span><!-- Star image -->
            </div>
            <div class="description"> <p>@Model.Description</p> </div>
            <div><p>*</p> @Address.fieldsToAddress(@Model) </div>
            <div><p>*</p> 9am-9pm, Mon-Fri </div>
        </div>
    </div>
    <br>
    <div class="phone">
        <h2> OrcaStars </h2>
        <div class="content">
            <img class="logo_photo" src="~/@Model.StoreLogoHolder" alt="logo_photo">
            <div>
                <h3>@Model.BusinessName</h3>
                <span>*</span><!-- Star image -->
            </div>
            <div class="description"> <p>@Model.Description</p> </div>
            <div><p>*</p> @Address.fieldsToAddress(@Model) </div>
            <div><p>*</p> 9am-9pm, Mon-Fri </div>
        </div>
    </div>
    <br>
</div>
